<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Location Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Camera and Audio -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Device Info -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- App Usage Stats -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />

    <!-- Basic Services -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />

    <!-- Background Services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Accessibility Service (for app blocking) -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <!-- Required to read browser history (legacy browsers) -->
    <uses-permission android:name="com.android.browser.permission.READ_HISTORY_BOOKMARKS" />

    <!-- System Alert Window -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Vibration -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Device Admin (for child mode protection) -->
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />

    <!-- Camera and sensors -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <application
        android:name=".ParentalControlApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParentalControl"
        tools:targetApi="33">

        <!-- Main Activities -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ParentalControl.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ModeSelectionActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ParentSetupActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ChildSetupActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ParentDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ChildDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ChildDetailsActivity"
            android:exported="false" />

        <activity
            android:name=".activities.LocationTrackerActivity"
            android:exported="false" />

        <activity
            android:name=".activities.AppBlockerActivity"
            android:exported="false" />

        <activity
            android:name=".activities.MediaGalleryActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ReportsActivity"
            android:exported="false" />

        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".activities.QRGeneratorActivity"
            android:exported="false" />

        <activity
            android:name=".activities.QRScannerActivity"
            android:exported="false" />

        <activity
            android:name=".activities.CalculatorActivity"
            android:exported="false" />

        <!-- Services -->
        <service
            android:name=".services.LocationTrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <service
            android:name=".services.AppUsageTrackingService"
            android:exported="false"
            android:permission="android.permission.PACKAGE_USAGE_STATS" />
        <service
            android:name=".services.CameraService"
            android:exported="false" />
        <service
            android:name=".services.AudioService"
            android:exported="false" />
        <service
            android:name=".services.CallSmsService"
            android:exported="false" />
        <service
            android:name=".services.CommandService"
            android:exported="false" />

        <service
            android:name=".services.NotificationListenerService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".services.ScreenCaptureService"
            android:exported="false" />

        <service
            android:name=".services.CompleteSOSService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <service
            android:name=".services.CompletePDFReportService"
            android:exported="false" />

        <service
            android:name=".services.AccessibilityBlockingService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".services.EnvironmentMonitoringService"
            android:exported="false" />
        <service
            android:name=".services.BrowserHistoryService"
            android:exported="false" />
        <service
            android:name=".services.ConnectivityService"
            android:exported="false" />

        <!-- Broadcast Receivers -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receivers.DeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policy" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receivers.AlarmReceiver"
            android:exported="true" />

        <!-- Providers -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
